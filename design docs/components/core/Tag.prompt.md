Quiet metadata chip — categories, sectors, filters. Lower emphasis than Badge.

```jsx
<Tag>SaaS</Tag>
<Tag active>Healthcare</Tag>
<Tag removable onRemove={() => {}}>Series B</Tag>
```

Use for filter sets and taxonomies. `active` gives a forest fill (selected). `removable` adds a dismiss button.
